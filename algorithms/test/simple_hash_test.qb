# 기본 문법 확인 테스트
println("=== # 주석 테스트 ===");

# 변수 선언
x = 42;
y = 3.14;

# 숫자만 출력 (문자열 연결 없이)
println(x);
println(y);

# 문자열만 출력
println("x 값을 출력했습니다");
println("y 값을 출력했습니다");

# 함수 테스트
fn test_function() {
    # 함수 내부 주석
    println("함수가 호출되었습니다");
    return 100;
}

result = test_function();
println(result);

# 조건문 테스트  
if (x > 40) {
    println("조건문 성공");
}

# 반복문 테스트
for i in range(1, 3) {
    println("반복문 실행 중");
}

println("테스트 완료!");