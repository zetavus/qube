# 렉서 디버그 테스트
println("디버그 시작");

# 1. 일반 문자열 (정상 작동해야 함)
println("일반 문자열 테스트");

# 2. 변수만 (정상 작동해야 함)  
name = "test";
println(name);

# 3. 가장 간단한 템플릿 (여기서 오류 발생 예상)
println("${name}");

println("디버그 완료");